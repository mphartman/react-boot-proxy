import React from 'react'

    const Contacts = ({ contacts }) => {
      return (
        <div>
          <center><h1>{contacts.greeting}</h1></center>
        </div>
      )
    };

    export default Contacts